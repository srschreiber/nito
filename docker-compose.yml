services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: nito
      POSTGRES_USER: nito
      POSTGRES_PASSWORD: nito-dev
    ports:
      - "5432:5432"
    volumes:
      - nito-db:/var/lib/postgresql/data

volumes:
  nito-db: